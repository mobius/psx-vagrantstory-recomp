# 状态：实验原型，暂停于提交准备

更新：2026-09-10。用户要求暂停继续开发并整理仓库。

## 已有结果

- 主程序、BIOS 和 Windows 可执行文件构建成功。
- 光盘 21 个参考程序摘要匹配；20 个非空 PRG 已有静态提取路径。
- 主程序种子 644 个；44 个带观测证据的 BATTLE 中部入口，另有两个已单独验证的入口。
- 本地缓存 117 个动态库；数量不等于完整函数或全游戏覆盖率。
- 确认真正进入 New Game：启动返回值为 0，区别于标题超时演出。
- 可操作 HUD、角色移动、跳跃、镜头旋转和第一房间正常开门切换已验证。
- 主菜单、Data、地图、Quick Manual 已验证；空白测试记忆卡正确返回无游戏存档。
- 运行时快照保存/恢复已验证，不等同游戏自身记忆卡存读档。
- PCSX-Redux 便携 CLI 完成 18000 帧本地参考采集，未使用全网卡网络调试服务。

## 尚未通过的验收

- 对敌人的攻击、伤害、击败和战斗奖励。
- 在法阵完成游戏存档写入，再重启并从记忆卡加载。
- 可见窗口呈现、音频和控制器实机体验。
- 其它地图、结局、完整流程和所有代码变体的原生覆盖。
- 新进程的后台自动捕获/编译全链路；手动编译已验证。

仍存在解释执行回退。累计调度计数包含诊断期间禁用原生路径的时间，不应当作性能结论。历史覆盖报告只有有限样本，有些不包含主程序调度表，不能用作完成百分比。

## 本机恢复点

当前在 Wine Cellar 的 Worker’s Breakroom（房间标识 `9/16`），靠近蓝色法阵。测试进程已停止。最新运行时检查点为本地测试存档目录的 slot 7；原游戏记忆卡没有已验证的新存档。

本机恢复步骤见最新 `docs/plan/*-handoff.md`；私有产物清单位于 `local/artifacts/`。这些本地目录不纳入提交或发行附件。
