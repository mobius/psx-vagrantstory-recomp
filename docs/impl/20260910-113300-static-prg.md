# 全PRG静态提取和编译
20个非空PRG与公开反编译SHA1全部匹配。提取2604个候选根，C/汇编候选区段共912692字节；自动链接数据仍由上游逐函数审计，不把C区段整体视为纯代码。
MAINMENU文件尾有字节对齐数据，最后不足4字节不作指令根。4项边界/摘要单元测试通过。
编译结果：62库成功，729项候选或缓存路径跳过，0失败。运行时重扫加载BATTLE主库（1875候选），总注册1949；具体运行是否正确仍需对照。
按键实测：FFF7进入Pad缓冲区0041f7ff，游戏vs_main_buttonsState为00080000，符合原库字节交换后的Start=0x800。此前字节序疑问不成立。
运行停在Have you found Sydney / Not yet, sir对白，帧持续推进；快照保存与恢复均返回last_ok=1，但不能代替游戏记忆卡存读档验收。
PCSX-Redux网络服务被自动审查拒绝（默认全网卡监听）；已改为明确关闭WebServer/GDB、便携目录、本地Lua截图，无网络对照接口。
