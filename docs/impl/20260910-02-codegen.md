# 代码生成与构建迭代
PSXRecomp 游戏/Bios 生成器以 GCC 13.2.0 构建成功。
PYTHONUTF8=1 修复中文 Windows 上子进程输出 GBK 解码异常；再次运行成功。
产出 32 分片，1813417 行，2055 dispatch entries。存在 143 处保留操作码提示；上游认为可能为数据被识别为代码，不能作为已正确执行证明。
光盘清单脚本读取 5162 文件，与 rood-reverse 的 21 个程序 SHA1 全部匹配。
物理内存经 Windows API 核实为 128 GiB。
运行库首次配置失败：旧 Ninja 版本探测为空。正在用本地 uv 环境的 CMake/Ninja 替代。
尚未生成经过运行验收的 Windows 游戏。

项目环境已安装 cmake 4.4.3、ninja 1.13.2。配置完成；额外关闭 PSX_REWIND（倒带功能缺子模块），保留单机核心。OpenBIOS 与 SCPH1001 后端均已链接配置。开始 -j16 原生程序编译。
脚本语法检查通过；游戏镜像、BIOS、生成代码确认被 gitignore 排除；可提交文档与工具的凭证模式扫描无命中。
