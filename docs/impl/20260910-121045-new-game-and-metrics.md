# New Game确认与增量指标
probe_new_game.py成功：Start在启动后即送入，frame3596确认D_80061068=0100、stateFlags.unkD=0；随后vs_main_startState=0，titleScreenCount=1，符合New Game而非超时演出。
已通过正常输入推进至地窖剧情。单帧相似画面不能证明停滞；此前“缓存导致停滞”仅是假设，未确证，不作为缺陷结论。
后续16个入口编译成功，新增总32个观察入口；加最初2个，补齐34个显式中部入口。缓存总105 DLL。无candidate/range-index overflow。
图形相关主程序rcos/rsin已原生化，两个初始热点的区间解释计数增量为0。其它计数需按区间评估，累计统计包括早先关闭原生路径的诊断时间，不能当当前性能或全游戏覆盖率。
仍待：第一次可操作场景、战斗、游戏自身记忆卡存读档、音频、后续地图；无窗口截图验证不覆盖实际音频与可见窗口呈现。
