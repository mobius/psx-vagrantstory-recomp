# 文本框结束条件验证
当前脚本PC80110467，字节11 00 01 00，对应原BATTLE处理器表800E9D30的opcode11 -> vs_battle_script_showTextBox(800B6AB0)。box0(800F4FE0)的speed=30、prevChunksPrinted=29、done=1；脚本等待speed为0或FFFF。
在同一slot3快照分别测试：overlay_native_off+set_input BFFF，speed立即变FFFF；恢复同一快照，overlay_native_on+set_input BFFF，speed同样变FFFF。游戏按键状态为0x40。两条路径一致，未证实原生文本框缺陷。
正常Cross是此剧情的关闭对白键；Circle用于标题菜单确认。advance_dialogue默认改为cross，并用按住0.25秒、释放0.75秒形成明确边沿。此前将静止对白直接归因为原生停滞的假设不成立。
下一步以实际当前房间D_800F1AB0和操作响应为验收，而非仅D_80061068的请求房间编号。
