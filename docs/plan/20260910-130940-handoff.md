# 提交准备与恢复说明

用户已要求停止进一步重编译/游玩测试，整理到 https://github.com/mobius/psx-vagrantstory-recomp 。目标仓库只读检查无 refs，当前准备本地初始提交，不推送。

## 恢复点

- 测试进程已关闭，最后检查点 slot 7 保存成功。
- 房间：Wine Cellar / Worker’s Breakroom，内部标识 9/16。
- 角色最后位置约 (640, 0, 380)，蓝色法阵附近。
- 私有检查点：`local/newgame-probe/saves/scph1001/state_8001F544_slot07.pst`。
- 最后画面：`local/handoff-screen.png`。
- 真正游戏存档写入尚未完成；测试记忆卡只验证了空卡读取。

后续若获准继续开发，可在项目目录运行：

```powershell
./tools/run.ps1 -Headless -MemcardDir local/newgame-probe/saves
# 运行时诊断端口就绪后，在另一终端：
.venv/Scripts/python.exe tools/debug_command.py '{"cmd":"savestate","op":"load","slot":7}'
.venv/Scripts/python.exe tools/debug_command.py '{"cmd":"savestate_status"}'
```

下一验收：站稳法阵后完成游戏 Data/Save 写入并重启加载，然后进入 Hall of Struggle 验证敌人战斗。不要把运行时快照当记忆卡存档。

## 提交边界

保留自有脚本、配置、地址种子、历史文档与三个固定版本子模块。游戏/BIOS输入、生成C、EXE/DLL、缓存、截图、捕获和存档全部留在忽略目录。历史文档保留当时假设；当前结论以根目录 STATUS.md 为准。

整理新增 bootstrap、输入校验、提交审计、仅本地产物打包工具。CMake配置和构建已通过；输入准备验证21个参考程序摘要匹配。Python语法、PowerShell语法、4个区段测试和5个审计自测已通过。

最终Git索引审计及产物校验结果见对应提交记录/本地 `local/artifacts/latest.json`。此轮不运行后续玩法测试。
