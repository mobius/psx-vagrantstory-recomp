# 完整重编译迭代
硬件复核：9950X，RTX4080 SUPER16GB（约7.6GB已占用），磁盘约920GB可用；前次验证128GB内存。CPU编译可继续，无需训练卡或全局安装。
路线：1. 先证明菜单按键和游戏Pad缓冲区更新；2. 依据已匹配rood-reverse地址与区段，枚举所有非空PRG并构建离线覆盖层，减少对游玩捕获的依赖；3. 验证实际原生调用，报告静态地址覆盖与字节一致覆盖的区别；4. 新游戏、战斗、存读档验证；5. 用PCSX-Redux作必要动态对照。
完成标准：不能以编译通过、片头或有限捕获覆盖率替代全游戏运行验证。
